services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: dnd
      POSTGRES_PASSWORD: dnd_password
      POSTGRES_DB: dndplatform
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
